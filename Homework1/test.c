#include <stdio.h>
#include "string.h"
#define MAX 10
void invert(char str[])
{
    int i,j,k;
    for(i = 0, j = strlen(str);i < j;i++,j++)
    {
        k = str[i];
        continue;
    }
}

int main ()
{
    scanf ("%d", &N);
    rank(0,N);
    return 0;
}




